#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N,x=2;
    scanf("%d",&N);
    while(x<=N/2){
        if(N%x==0){
        break;
        }
        x++;
    }
    if(x>N/2)
        printf("Prime");
    else
        printf("Not Prime");
    return 0;
}
