#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    scanf("%d",&N);
    int original = N;
    if(N>=0 && N<=9){
    }
        int r=0;
        while(N!=0){
            int last = N%10;
                N = N/10;
                r = r*10 + last;
        }
        if(original==r){
            printf("Palindrome\n");
        }
    else{
        printf("Not Palindrome");
    }
    
    return 0;
}
