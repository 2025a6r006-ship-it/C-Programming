#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    scanf("%d",&N);
    
    int a[N];
    for(int i=0;i<N;i++){
        scanf("%d",&a[i]);
    }
    
    for(int i=0;i<N;i++){
        int isduplicate=0;
        
      for(int j=0;j<i;j++){
          if(a[i]==a[j]){
              isduplicate=1;
              break;
          }
      }
        if(!isduplicate){
            printf("%d ",a[i]);
        }
    }
    
    return 0;
}
