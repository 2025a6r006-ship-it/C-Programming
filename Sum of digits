#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N,x,sum=0;
    scanf("%d",&N);
    while(N>0){
        x=N%10;
        N=N/10;
    sum = sum+x;
    }
    printf("%d",sum);
    return 0;
}
