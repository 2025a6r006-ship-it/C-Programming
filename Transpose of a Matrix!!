#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int R,C;
    scanf("%d%d",&R,&C);
    int a[R][C];
    for(int i=0;i<R;i++){
        for(int j=0;j<C;j++){
            scanf("%d",&a[i][j]);
        }
    }
    
    for(int i=0;i<C;i++){
        for(int j=0;j<R;j++){
            printf("%d",a[j][i]);
            if(j!=R-1)
                printf(" ");
        }
        printf("\n");
    }
    return 0;
}
